# Server-only — never expose to the client
GEMINI_API_KEY=

# Safe to expose to the client (anon key is public-by-design; RLS is the real gate)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
